public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here
    if (con.AccountId == opp.AccountId && con.AccountId == acc.Id && con.AccountId !=null && opp.AccountId != null){
        return true; 
       
    }
    else {
        return false; 
    }
}