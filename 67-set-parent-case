public void linkParent(Case c1, Case c2) {
    //code here
    if (c1.ContactId == c2.ContactId && c1.CreatedDate > c2.CreatedDate){
        c1.Id = c2.ParentId; 

    }
    else if (c1.ContactId == c2.ContactId && c1.CreatedDate < c2.CreatedDate){
        c2.Id = c1.ParentId; 
    
    }
}